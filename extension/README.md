# Chrome Extension

The current working extension will be migrated here after its local v2.2 files are committed/synchronized.

Its next integration point is the ingestion API: instead of treating download as the terminal action, it will submit each discovered media candidate and display statuses such as NEW, DUPLICATE_URL, DUPLICATE_HASH, STORED and ERROR.
